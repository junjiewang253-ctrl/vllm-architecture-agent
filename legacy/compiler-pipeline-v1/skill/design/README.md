# Architecture Design Layer

The Design Layer is the v1.1 bridge between Architecture Concepts and
Architecture View Graph.

Concepts answer what source facts mean. Design answers how a page should explain
the system: page question, primary flow, branches, external boundaries,
annotations, strategies and mapping lanes.

Renderers must not consume concepts directly.
